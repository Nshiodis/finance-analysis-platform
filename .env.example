# Day32 配置模板：复制为 .env 后按需修改
# 环境：development / testing / production
FINANCE_ENVIRONMENT=development

# 日志级别：DEBUG < INFO < WARNING < ERROR（INFO 会显示请求日志）
FINANCE_LOG_LEVEL=INFO

# 以下可选覆盖，不写则用 config.py 里的默认路径
# FINANCE_DATABASE_PATH=
# FINANCE_DATA_PATH=
# FINANCE_OUTPUT_PATH=
# FINANCE_LOG_PATH=